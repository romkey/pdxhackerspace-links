<% content_for :title, "Printers" %>

<%= settings_frame do %>
  <div class="d-flex align-items-end justify-content-between mb-3">
    <div>
      <h1 class="h-page-title mb-0">Printers</h1>
      <div class="text-13 text-secondary mt-1">
        <span class="text-body"><%= @printers.size %></span> total ·
        <span class="text-body"><%= @printers.count(&:enabled?) %></span> enabled
      </div>
    </div>
    <div>
      <%= link_to "+ Add printer", new_settings_printer_path, class: "btn btn-primary btn-sm" %>
    </div>
  </div>

  <% if @printers.any? %>
    <div class="table-responsive">
      <table class="table table-compact table-hover align-middle mb-0">
        <thead>
          <tr>
            <th>Name</th>
            <th>Type</th>
            <th>Connection</th>
            <th>Page size</th>
            <th>Status</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @printers.each do |printer| %>
            <tr class="clickable-row" data-href="<%= settings_printer_path(printer) %>">
              <td class="fw-medium"><%= printer.name %></td>
              <td class="text-secondary"><%= printer.printer_type_label %></td>
              <td class="text-secondary text-12">
                <% if printer.cups? %>
                  <%= printer.cups_name %> @ <%= printer.cups_server %>
                <% else %>
                  <code class="text-12"><%= truncate(printer.print_command, length: 48) %></code>
                <% end %>
              </td>
              <td class="text-secondary">
                <% if printer.cups? %>
                  <%= printer.page_size_label %>
                  <% if printer.avery_template.present? %>
                    <span class="text-12"> · <%= printer.avery_template_label %></span>
                  <% end %>
                <% else %>
                  <%= printer.label_height_mm %> mm strip
                <% end %>
              </td>
              <td>
                <% if printer.enabled? %>
                  <span class="status-dot status-success" title="Enabled"></span>
                <% else %>
                  <span class="text-bg-secondary-subtle badge">Disabled</span>
                <% end %>
              </td>
              <td class="text-end">
                <%= link_to "Edit", edit_settings_printer_path(printer), class: "btn btn-outline-secondary btn-sm" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="text-secondary text-13 mb-0">No printers yet. Add a CUPS queue or a command printer that runs a script on generated PNG labels.</p>
  <% end %>
<% end %>

<script>
  document.querySelectorAll(".clickable-row").forEach((row) => {
    row.addEventListener("click", (event) => {
      if (event.target.closest("a, button")) return;
      window.location = row.dataset.href;
    });
  });
</script>
